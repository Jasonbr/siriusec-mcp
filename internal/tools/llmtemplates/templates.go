// Package llmtemplates 提供优化的 LLM 输出模板
package llmtemplates

// DiagnosisReportTemplate 诊断报告精美模板
const DiagnosisReportTemplate = `
# 🏥 智能诊断报告

**生成时间**: {{.Time}}  
**诊断模型**: {{.Model}}  
**症状**: {{.Symptom}}

---

## 📊 实时系统状态

{{if .MetricsTable}}
| 指标 | 当前值 | 阈值 | 状态 | 趋势 |
|------|--------|------|------|------|
{{range .MetricsTable}}| {{.Name}} | {{.Value}} | {{.Threshold}} | {{.Status}} | {{.Trend}} |
{{end}}
{{else}}
*正在获取实时数据...*
{{end}}

---

## 🔍 根本原因分析

### 主要原因（按概率排序）

| 概率 | 可能原因 | 关键特征 | 验证命令 | 置信度 |
|:----:|----------|---------|---------|:------:|
| 🔴 高 | CPU 密集型进程死循环 | load 高，wa 低 | ` + "`top -bn1 | head -20`" + ` | 85%% |
| 🟡 中 | 定时任务集中执行 | crontab 配置不当 | ` + "`crontab -l`" + ` | 60%% |
| 🟢 低 | 病毒/挖矿程序 | 异常进程名 | ` + "`ps auxf`" + ` | 30%% |

---

## 🎯 快速诊断步骤（5 分钟内）

### Step 1: 确认问题类型 ⏱️ 30 秒

` + "```bash" + `
# 查看系统负载
uptime

# 快速浏览进程
top -bn1 | head -20
` + "```" + `

**判断标准**:
- ✅ 如果 ` + "`wa > 20%%`" + ` → **IO 瓶颈**
- ✅ 如果 ` + "`id < 10%%`" + ` → **CPU 瓶颈**
- ✅ 如果 load 高但 CPU 空闲 → **D 状态进程堆积**

### Step 2: 定位问题进程 ⏱️ 2 分钟

` + "```bash" + `
# 按 CPU 排序找罪魁祸首
ps aux --sort=-%cpu | head -10

# 按内存排序
ps aux --sort=-%mem | head -10

# 查看异常 D 状态进程
ps aux | awk '$8 ~ /D/'
` + "```" + `

### Step 3: 深度分析 ⏱️ 3 分钟

` + "```bash" + `
# IO 性能分析
iostat -xz 1 3

# 内存使用详情
free -h && cat /proc/meminfo | grep -E "Mem|Swap"

# 网络连接数
ss -s | head -10
` + "```" + `

---

## 💡 解决方案

### 🚨 紧急处理（立即执行）

{{if .UrgentActions}}
{{range .UrgentActions}}
**操作**: {{.Action}}
` + "```bash" + `
{{.Command}}
` + "```" + `
**预期效果**: {{.ExpectedResult}}

{{end}}
{{else}}
*根据实际诊断结果生成具体命令*
{{end}}

### 🔧 中期优化（本周内）

1. **资源配置优化**
   - 调整进程优先级：` + "`nice`" + ` / ` + "`renice`" + `
   - 限制资源使用：cgroup 配置
   
2. **架构改进**
   - 负载均衡分散压力
   - 水平扩容增加实例

### 📈 长期预防（持续进行）

| 措施 | 实施难度 | 预期效果 | 时间投入 |
|------|---------|---------|---------|
| 部署监控系统 | ⭐⭐ | 提前预警 | 2 天 |
| 优化代码逻辑 | ⭐⭐⭐ | 根除问题 | 1 周 |
| 容量规划 | ⭐⭐ | 避免过载 | 持续 |

---

## 📈 可视化分析

### CPU 使用率趋势
` + "```" + `
[██████████░░] 85% 用户态
[██░░░░░░░░░░] 15% 内核态  
[░░░░░░░░░░░░] 0% 等待
` + "```" + `

### 内存分布
` + "```" + `
已用：[████████░░░░░░░░] 6.2G / 8G (77%)
缓存：[██░░░░░░░░░░░░░░] 1.1G (14%)
空闲：[░░░░░░░░░░░░░░░░] 0.7G (9%)
` + "```" + `

### 负载对比（1/5/15 分钟）
` + "```" + `
Load1:  [████████████░░░░] 12.5
Load5:  [██████████░░░░░░] 10.2  
Load15: [████████░░░░░░░░] 8.3
` + "```" + `

---

## ⚠️ 风险评估

| 风险项 | 严重性 | 可能性 | 影响范围 | 缓解措施 |
|--------|:------:|:------:|---------|---------|
| 服务中断 | 🔴 高 | 中 | 全部用户 | 立即扩容 |
| 数据丢失 | 🟡 中 | 低 | 部分用户 | 备份恢复 |
| 性能下降 | 🟢 低 | 高 | 体验受损 | 限流降级 |

---

## 📚 相关文档

- [Linux 性能分析最佳实践](docs/linux-perf.md)
- [CPU 问题排查手册](docs/cpu-troubleshooting.md)
- [内存管理指南](docs/memory-management.md)

---

**💡 提示**: 
- 🔴 标红项目需要立即关注
- ⏱️ 时间估算基于典型场景
- 建议按顺序执行诊断步骤

**🔄 下一步**: 
如需更详细分析，请提供上述诊断命令的输出结果。
`
